<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dyj.requestpagingscheduleemulator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RequestPagingScheduleEmulator">
        <activity
            android:name=".module.eatwhat.activity.EatManageActivity"
            android:exported="false" />
        <activity
            android:name=".module.eatwhat.activity.EatActivity"
            android:exported="false" />
        <activity
            android:name=".module.main.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>